websockets>=12.0
sounddevice>=0.4.6
numpy>=1.24.0,<2.0.0
vosk>=0.3.45
openwakeword>=0.6.0

# Whisper STT backend (for server_whisper.py / engine_whisper.py)
# faster-whisper is preferred (faster, lower memory); openai-whisper is the fallback
# when faster-whisper DLL dependencies are missing on Windows.
# Install at least one:
faster-whisper>=0.10.0
# openai-whisper>=20231117   # uncomment if faster-whisper DLL fails on Windows
